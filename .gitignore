# ROS2 / colcon build artifacts — never commit these
# Each machine (laptop, Pi) generates its own via: colcon build
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Editor
*.user
*~
.#*
.project
.cproject
qtcreator-*

# ROS1 leftovers (not used here, kept for safety)
devel/
devel_isolated/
build_isolated/
